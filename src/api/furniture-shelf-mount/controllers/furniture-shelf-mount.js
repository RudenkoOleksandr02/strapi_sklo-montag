'use strict';

/**
 * furniture-shelf-mount controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::furniture-shelf-mount.furniture-shelf-mount');
