'use strict';

/**
 * furniture-shelf-mount service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::furniture-shelf-mount.furniture-shelf-mount');
