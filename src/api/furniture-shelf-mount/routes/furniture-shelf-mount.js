'use strict';

/**
 * furniture-shelf-mount router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::furniture-shelf-mount.furniture-shelf-mount');
